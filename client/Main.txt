import React, { useState } from "react";
import { Link } from "react-router-dom";
import { FiCalendar, FiPlus, FiDollarSign, FiShoppingCart, FiBox, FiCreditCard } from "react-icons/fi";

function Main() {
    const [activeButton, setActiveButton] = useState("all-time");

    const timeRangeButtons = [
        { id: "all-time", label: "All Time" },
        { id: "12-months", label: "12 Months" },
        { id: "30-days", label: "30 Days" },
        { id: "7-days", label: "7 Days" },
        { id: "24-hour", label: "24 Hour" },
    ];

    const statsData = [
        {
            title: "Total Revenue",
            icon: <FiDollarSign className="text-[#5C59E8] w-6 h-6" />,
            bg: "bg-[#9D9BF1]",
            amount: "$25,400",
            label: "+15%",
            isIncrease: true,
        },
        {
            title: "Total Sales",
            icon: <FiShoppingCart className="text-[#0D894F] w-6 h-6" />,
            bg: "bg-[#CFE7DC]",
            amount: "$3,200",
            label: "+8%",
            isIncrease: true,
        },
        {
            title: "Product SKU",
            icon: <FiBox className="text-[#F04438] w-6 h-6" />,
            bg: "bg-[#FEEDEC]",
            amount: "$1,560",
            label: "-5%",
            isIncrease: false,
        },
        {
            title: "Balance",
            icon: <FiCreditCard className="text-[#E46A11] w-6 h-6" />,
            bg: "bg-[#FAE1CF]",
            amount: "$12,000",
            label: "+10%",
            isIncrease: true,
        },
    ];

    return (
        <div className="p-4 bg-gray-50 min-h-screen">
            {/* Time Range and Action Buttons */}
            <div className="flex flex-wrap justify-between items-center bg-white p-4 shadow-md rounded-lg mb-6">
                {/* Time Range Buttons (Left Side) */}
                <div className="flex flex-wrap items-center space-x-2">
                    {timeRangeButtons.map((button) => (
                        <Link
                            key={button.id}
                            // to={`/analytics/${button.id}`}
                            className={`px-4 py-2 rounded-lg text-sm font-medium transition-all duration-300 ${activeButton === button.id
                                ? "bg-primary text-white"
                                : "bg-gray-100 text-gray-700 hover:bg-primary hover:text-white"
                                }`}
                            onClick={() => setActiveButton(button.id)}
                        >
                            {button.label}
                        </Link>
                    ))}
                </div>

                {/* Right Side Buttons */}
                <div className="flex flex-wrap items-center space-x-2 mt-4 md:mt-0">
                    {/* Select Dates Button */}
                    <button className="flex items-center space-x-2 px-4 py-2 bg-gray-100 text-gray-700 hover:bg-primary hover:text-white rounded-lg text-sm font-medium transition-all duration-300">
                        <FiCalendar className="w-5 h-5" />
                        <span>Select Dates</span>
                    </button>

                    {/* Add Product Button */}
                    <button className="flex items-center space-x-2 px-4 py-2 bg-primary text-white hover:bg-blue-600 rounded-lg text-sm font-medium transition-all duration-300">
                        <FiPlus className="w-5 h-5" />
                        <span>Add Product</span>
                    </button>
                </div>
            </div>

            {/* Statistic Boxes */}
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
                {statsData.map((stat, index) => (
                    <div
                        key={index}
                        className="flex flex-col items-center justify-center bg-white shadow-md rounded-lg p-4"
                        style={{ width: "264px", height: "160px" }}
                    >
                        {/* Icon Circle */}
                        <div className={`w-12 h-12 rounded-full ${stat.bg} bg-opacity-40 flex items-center justify-center mb-3`}>
                            {stat.icon}
                        </div>

                        {/* Title */}
                        <h3 className="text-sm font-medium text-gray-700 mb-1">{stat.title}</h3>

                        {/* Amount and Label */}
                        <div className="flex items-center space-x-2">
                            <p className="text-xl font-semibold text-gray-900">{stat.amount}</p>
                            <span
                                className={`text-sm ${stat.isIncrease ? `bg-[#CFE7DC]` : 'bg-[#FCDAD7]'} rounded-lg w-[40px] h-[20px] text-center font-medium ${stat.isIncrease ? "text-green-500" : "text-red-500"
                                    }`}
                            >
                                {stat.label}
                            </span>
                        </div>
                    </div>
                ))}
            </div>
            <div className="my-4 grid grid-cols-1  lg:grid-cols-2 gap-4">
                <div
                    className="flex flex-col items-center justify-center bg-gray-100 shadow-md rounded-lg p-4"
                    style={{ width: "264px", height: "160px" }}
                >

                </div>
            </div>

        </div>
    );
}

export default Main;
